#include <stdio.h>

int main(void)
{
    // int a;
    // a = getchar();
    // putchar(a);

    // printf("SDDFFFFFF");
    char i;
    while((i = getchar()) != '\n')
        putchar(i);
    return 0;
}